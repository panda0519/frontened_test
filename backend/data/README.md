# Backend Data

This directory contains CSV files used by the backend.

- `stations.csv`: Metadata for monitoring stations.
- `processed_math_data.csv`: Historical data readings.
- `ml_forecast_results.csv`: Predicted values.

Please ensure these files are kept updated as needed.
